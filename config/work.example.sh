# Copy this file to config/work.sh for local work-specific shell settings.
# Do not commit config/work.sh.
#
# Keep company names, private hosts, tokens, and credentials out of this repo.

export WORKSPACE_DIR="$HOME/Developer"

